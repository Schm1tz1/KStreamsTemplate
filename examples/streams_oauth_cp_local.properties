# General Kafka and Streams
bootstrap.servers=kafka.confluent.svc.cluster.local:9092
acks=all
replication.factor=3
num.standby.replicas=1
#num.stream.threads=3

# Exactly-once processing (transactional producer + fenced zombie instances)
processing.guarantee=exactly_once_v2

# SASL/OAUTHBEARER against an OIDC-compatible token endpoint (client_credentials grant)
security.protocol=SASL_SSL
sasl.mechanism=OAUTHBEARER
sasl.login.callback.handler.class=org.apache.kafka.common.security.oauthbearer.OAuthBearerLoginCallbackHandler
sasl.oauthbearer.token.endpoint.url=http://keycloak.confluent.svc.cluster.local:8080/realms/sso_test/protocol/openid-connect/token
sasl.jaas.config=org.apache.kafka.common.security.oauthbearer.OAuthBearerLoginModule required \
   clientId="client" \
   clientSecret="QvCcQKUsYEoFZuCiK4ahxnzNDSdZXUyp";

# Trust the broker's self-signed certificate via a CA bundle in PEM format,
# without having to import it into a JKS/PKCS12 keystore first.
ssl.truststore.type=PEM
ssl.truststore.location=/app/certs/ca.crt

# Pipeline
streamsApp.inputTopic=kstreams-input-topic
streamsApp.outputTopic=kstreams-output-topic
